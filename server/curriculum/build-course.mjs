import { mkdirSync, readFileSync, writeFileSync } from "node:fs";
import { dirname, resolve } from "node:path";
import { fileURLToPath } from "node:url";
const __dirname = dirname(fileURLToPath(import.meta.url));
const serverRoot = resolve(__dirname, "..");
const repoRoot = resolve(serverRoot, "..");
const generatedDir = resolve(serverRoot, "generated");
const dbGeneratedDir = resolve(repoRoot, "db", "generated");
const courseId = "bolo-english-complete-system";
const version = "2026-03-24";
const repetitionDays = [1, 3, 7, 14, 30];
const blueprints = [1, 2, 3, 4].map((level) => JSON.parse(readFileSync(resolve(__dirname, `blueprint-level-${level}.json`), "utf8")));
const systems = { grammar_system: { design_principles: ["Grammar appears in practical speaking situations before it is named explicitly.", "Each grammar topic returns in later lessons through conversation, writing, and quiz tasks.", "Learners move from controlled patterns to freer communication at every level."], coverage: ["parts of speech", "sentence structure", "simple present, present continuous, simple past, past continuous, past perfect", "future forms with will and going to", "present perfect and present perfect continuous", "articles and quantifiers", "prepositions and direction language", "comparatives and superlatives", "modals for ability, permission, advice, obligation, certainty, speculation", "conditionals from basic to advanced", "active and passive voice", "direct and indirect speech", "relative clauses, noun clauses, gerunds, infinitives", "complex sentence connectors", "formal and professional English usage"] }, vocabulary_system: { target_exposures: 2000, categories: ["Daily life", "Business", "Travel", "Emotions", "Work", "Social", "Health", "Technology", "Leadership", "Community"], approach: "Each lesson introduces 10 structured words or phrases, mixes them with review items, and ties them to one communicative task.", revision_cycles: ["same lesson review", "next lesson retrieval", "end-of-chapter recap", "end-of-level spiral review", "30-day spaced recall"], spaced_repetition_days: repetitionDays }, speaking_system: { daily_speaking_prompt_model: "Every lesson includes one focused speaking task, one role-play context, one confidence instruction, and one think-in-English drill.", role_play_ladders: ["guided repetition", "supported role-play", "personalized response", "timed fluency response", "real-world simulation"], confidence_builders: ["chunk speaking instead of word-by-word translation", "content before perfection", "repeat with one improvement", "speak with pauses, not panic", "finish the thought before self-correcting"] }, progression_system: { unlock_logic: { first_lesson_unlocked_by_default: true, next_lesson_unlocks_after_completion: true, next_chapter_unlocks_after_all_chapter_lessons_completed: true, next_level_unlocks_after_previous_level_completed: true }, mastery_rules: ["Mark lesson complete after speaking task, writing task, and quiz are submitted.", "Recommend review when quiz score is below 70%.", "Promote to the next level after 80% lesson completion plus capstone completion."] } };
const titleCase = (value) => value.charAt(0).toUpperCase() + value.slice(1);
const levelHindiSupport = (level) => level === 1 ? "Hindi support strong hai: rule ko Hindi explanation ke saath connect karo, phir English pattern ko zor se bolo." : level === 2 ? "Hindi se meaning samjho, lekin sentence English mein build karo. Translation ko step-by-step kam karo." : level === 3 ? "Hindi only as rescue support use karo. Default response English thought blocks mein do." : "Hindi support optional hai: nuance samajhne ke liye compare kar sakte ho, lekin production English-first rakho.";
function buildPatternBank(grammarTopic, skillFocus) { const topic = grammarTopic.toLowerCase(); if (topic.includes("be verb") || topic.includes("adjectives with be") || topic.includes("identity")) return ["I am ___.", "She is ___ and ___.", "We are from ___."]; if (topic.includes("present continuous")) return ["I am ___ing right now.", "They are ___ing at the moment.", "What are you ___ing now?"]; if (topic.includes("simple present") || topic.includes("routines") || topic.includes("habit")) return ["I ___ every day.", "She usually ___ before work.", "Do you ___ in the morning?"]; if (topic.includes("simple past") || topic.includes("was and were") || topic.includes("storytelling")) return ["I ___ yesterday.", "We were ___ last night.", "What did you ___ after that?"]; if (topic.includes("present perfect")) return ["I have ___ before.", "She has already ___.", "Have you ever ___?"]; if (topic.includes("future") || topic.includes("going to") || topic.includes("will")) return ["I will ___ tomorrow.", "We are going to ___ next week.", "What are you going to ___ next?"]; if (topic.includes("comparative")) return ["This is ___er than that.", "Option A is more ___ than option B.", "Which one is better for us?"]; if (topic.includes("modal") || topic.includes("can") || topic.includes("permission") || topic.includes("advice") || topic.includes("obligation")) return ["I can ___.", "You should ___ before ___.", "Do we have to ___ today?"]; if (topic.includes("conditional")) return ["If ___ happens, I will ___.", "If I had ___, I would ___.", "What would you do if ___?"]; if (topic.includes("passive")) return ["The report was completed on time.", "The issue is being reviewed now.", "The decision will be shared tomorrow."]; if (topic.includes("reported") || topic.includes("indirect speech")) return ["She said that she was ready.", "He told me to wait outside.", "They asked whether we could help."]; if (topic.includes("relative clauses")) return ["This is the person who helped me.", "I need a tool that works quickly.", "We visited a place where everyone felt welcome."]; if (topic.includes("gerunds") || topic.includes("infinitives")) return ["I enjoy ___ing after work.", "She decided to ___ early.", "We avoid ___ing without a plan."]; if (topic.includes("connector") || topic.includes("cause") || topic.includes("effect") || topic.includes("summary")) return ["First, ___; then, ___.", "Because ___, we decided to ___.", "However, we still need to ___."]; if (topic.includes("email") || topic.includes("message") || topic.includes("tone")) return ["I am writing to ___.", "Could you please ___ by ___.", "Thanks for your support on this."]; if (topic.includes("presentation") || topic.includes("data")) return ["Today, I would like to discuss ___.", "First, let us look at ___.", "The key takeaway is ___."]; return ["My main point is ___.", "One useful detail is ___.", "What would you add next?"]; }
function buildGrammarNotes(grammarTopic) { const topic = grammarTopic.toLowerCase(); if (topic.includes("pronunciation") || topic.includes("sound")) return { why_it_matters: "Clear sound awareness helps learners hear words correctly and speak with less fear.", key_points: ["Notice the sound before trying to speak fast.", "Repeat in short chunks, not whole paragraphs.", "Link sound to one useful word immediately."] }; if (topic.includes("simple present")) return { why_it_matters: "Simple present is the base of daily conversation because routines, habits, and facts appear constantly.", key_points: ["Use the base verb for I, you, we, they.", "Add s or es with he, she, it.", "Use do or does to ask and answer questions."] }; if (topic.includes("simple past")) return { why_it_matters: "Past forms let learners tell stories, share experiences, and explain what happened clearly.", key_points: ["Use the past verb for finished actions.", "Irregular verbs must be memorized through repeated use.", "Keep time markers clear: yesterday, last week, a few minutes ago."] }; if (topic.includes("present perfect")) return { why_it_matters: "Present perfect connects past experience to the present and is common in everyday and professional English.", key_points: ["Use have or has plus the past participle.", "Use it for experience, unfinished time, or recent results.", "Signal words like ever, never, already, and yet change the meaning."] }; if (topic.includes("conditional")) return { why_it_matters: "Conditionals help learners think about consequences, risks, and possibilities in a more advanced way.", key_points: ["Match the conditional type to reality or imagination.", "Keep the verb form consistent with the time meaning.", "Use conditionals to give advice, discuss strategy, or reflect on outcomes."] }; if (topic.includes("passive")) return { why_it_matters: "Passive voice is common when the action, result, or formal tone matters more than the actor.", key_points: ["Use a be verb plus a past participle.", "Use passive when the doer is unknown, obvious, or not important.", "Passive is common in reports, updates, and process explanations."] }; if (topic.includes("reported") || topic.includes("indirect speech")) return { why_it_matters: "Reported speech makes it possible to summarize conversations, instructions, and updates naturally.", key_points: ["Change tense when the reporting moment moves into the past.", "Adjust pronouns and time words when needed.", "Use tell, say, ask, explain, or mention accurately."] }; if (topic.includes("professional") || topic.includes("tone") || topic.includes("summary") || topic.includes("presentation")) return { why_it_matters: "Professional English depends on structure, tone, and precision as much as grammar accuracy.", key_points: ["State the point early.", "Use connectors to guide the listener or reader.", "Close with a result, recommendation, or next step."] }; return { why_it_matters: "This grammar topic helps the learner move from memorized words to usable speaking patterns.", key_points: ["Notice the pattern, not just one sentence.", "Repeat the pattern with personal meaning.", "Use the pattern in speaking before trying to explain the rule."] }; }
const buildExplanation = (level, grammarTopic, skillFocus) => ({ simple_english: `In this lesson, the learner uses ${grammarTopic} to ${skillFocus}. The goal is not only to know the rule but to say it in a short real-life conversation without freezing.`, hindi_support: levelHindiSupport(level) });
const buildPatternVocabulary = (patterns, lessonTitle) => patterns.slice(0, 2).map((pattern, index) => ({ term: pattern.replace(/___/g, "...").replace(/\s+/g, " ").trim(), meaning: index === 0 ? `a useful sentence frame for ${lessonTitle.toLowerCase()}` : "a repeatable phrase pattern for guided speaking", example: pattern.replace(/___/g, index === 0 ? "a clear idea" : "the right detail"), usage_context: "Sentence pattern", category: "Communication", review_bucket: "pattern" }));
function buildVocabulary(chapter, lessonIndex, patterns, level) { const items = []; const start = lessonIndex % chapter.lexicon.length; for (let offset = 0; offset < 6; offset += 1) { const [term, meaning] = chapter.lexicon[(start + offset) % chapter.lexicon.length]; items.push({ term, meaning, example: `Use "${term}" when ${chapter.contexts[lessonIndex % chapter.contexts.length]}.`, usage_context: chapter.category, category: chapter.category, review_bucket: offset < 4 ? "focus" : "recycle" }); } items.push(...buildPatternVocabulary(patterns, chapter.lessons[lessonIndex][0])); if (lessonIndex > 0) { const [term, meaning] = chapter.lexicon[(start + chapter.lexicon.length - 1) % chapter.lexicon.length]; items.push({ term: `${term} again`, meaning: `review of ${meaning}`, example: `Review "${term}" aloud before the main speaking task.`, usage_context: "Spaced review", category: chapter.category, review_bucket: "review" }); } items.push({ term: level <= 2 ? "think in English" : "say the idea directly", meaning: level <= 2 ? "build the message in English before translating word by word" : "express the point clearly without over-editing", example: level <= 2 ? "Think in English for one short line before speaking." : "Say the core idea first, then refine it.", usage_context: "Speaking mindset", category: "Communication", review_bucket: "mindset" }); return items.slice(0, 10); }
function buildConversation(lesson, chapter, patterns, level) { const scenario = chapter.contexts[lesson.order_index % chapter.contexts.length]; const politeLine = level <= 2 ? "Could you please help me with this?" : "Could we look at the best next step here?"; return [{ scenario: titleCase(scenario), dialogue: [{ speaker: "Learner", line: patterns[0].replace("___", "the key point") }, { speaker: "Partner", line: "Sure. What exactly do you need in this situation?" }, { speaker: "Learner", line: politeLine }, { speaker: "Partner", line: "That works. Let us handle it one step at a time." }] }, { scenario: `Personalized ${titleCase(chapter.category)} Task`, dialogue: [{ speaker: "Learner", line: `I want to ${lesson.learning_objective.toLowerCase()}.` }, { speaker: "Coach", line: "Good. Add one concrete detail and one clear sentence pattern." }, { speaker: "Learner", line: patterns[1].replace("___", "one useful detail") }, { speaker: "Coach", line: "Better. Now repeat it with a calmer pace." }] }]; }
const buildSpeakingTask = (lesson, patterns, level) => ({ prompt: `Speak for ${level <= 2 ? "45 to 60" : level === 3 ? "60 to 90" : "90 to 120"} seconds about this task: ${lesson.learning_objective}.`, steps: [`Start with this frame: ${patterns[0]}`, `Add one personal detail using this frame: ${patterns[1]}`, `Close with a final line using this frame: ${patterns[2]}`], success_criteria: ["Use at least two target words from the vocabulary list.", "Speak in short chunks instead of translating word by word.", "Finish the response even if one small mistake happens."], think_in_english_drill: level <= 2 ? "Say the idea in Hindi inside your head only once, then rebuild it in English using chunks." : "Think of the message in English keywords before you speak full sentences." });
const buildWritingTask = (lesson, patterns) => ({ prompt: `Write 5 to 8 lines to show that you can ${lesson.learning_objective.toLowerCase()}.`, checklist: [`Use this pattern at least once: ${patterns[0]}`, "Add one real detail from your life or work.", "End with one sentence that sounds confident and complete."] });
const buildMicroDrills = (patterns, vocabularyList) => [{ type: "pattern_substitution", instruction: `Replace the blank in this frame with three new ideas: ${patterns[0]}` }, { type: "vocabulary_retrieval", instruction: `Use any three of these items in one short answer: ${vocabularyList.slice(0, 3).map((item) => item.term).join(", ")}` }, { type: "fluency_repeat", instruction: "Say the same answer twice: first slowly, then naturally." }];
function buildQuiz(lesson, patterns, vocabularyList) { const vocabularyTerm = vocabularyList[0]; return [{ question: `Which sentence best matches the target pattern for ${lesson.title.toLowerCase()}?`, options: [patterns[0].replace("___", "one clear idea"), "I am speaking because future yesterday.", "Do can I to go?", "He are not readying plan."], correct_answer: patterns[0].replace("___", "one clear idea") }, { question: `What does \"${vocabularyTerm.term}\" mean in this lesson?`, options: [vocabularyTerm.meaning, "a difficult grammar exam", "a random sound with no meaning", "a polite way to end every email"], correct_answer: vocabularyTerm.meaning }, { question: `Which option uses ${lesson.grammar_topic.name} more accurately?`, options: [patterns[1].replace("___", "the right detail"), "I has go there every day.", "They was doing tomorrow.", "She do not can help."], correct_answer: patterns[1].replace("___", "the right detail") }, { question: "In a real conversation, what is the best response strategy for this lesson?", options: ["Use one clear sentence first, then add one supporting detail.", "Translate each word silently before every sentence.", "Wait for perfect grammar before speaking anything.", "Use only one-word answers."], correct_answer: "Use one clear sentence first, then add one supporting detail." }, { question: "Which phrase sounds most natural as a confident close?", options: [patterns[2].replace("___", "one final point"), "End finish because done.", "No idea maybe nothing.", "This sentence is breaked."], correct_answer: patterns[2].replace("___", "one final point") }]; }
function buildCommonMistakes(grammarTopic, lessonTitle) { const topic = grammarTopic.toLowerCase(); const mistakes = []; if (topic.includes("simple present")) mistakes.push("Forgetting the s or es ending with he, she, or it."); if (topic.includes("simple past")) mistakes.push("Using the base verb after already marking the sentence as past."); if (topic.includes("present continuous")) mistakes.push("Missing the be verb before the ing form."); if (topic.includes("articles")) mistakes.push("Skipping a, an, or the because Hindi often does not require the same pattern."); if (topic.includes("conditional")) mistakes.push("Mixing up the tense pair inside the if-clause and result clause."); if (topic.includes("passive")) mistakes.push("Using a passive sentence when an active sentence would sound simpler and clearer."); mistakes.push(`Speaking too fast during ${lessonTitle.toLowerCase()} and losing clarity.`); mistakes.push("Translating word by word instead of speaking in memorized chunks."); return mistakes.slice(0, 4); }
const buildConfidenceTip = (level, lesson) => level === 1 ? `For ${lesson.title.toLowerCase()}, speak in very short blocks. A complete short answer is stronger than a broken long answer.` : level === 2 ? "Do not chase perfect grammar first. Start with one correct frame, then extend it with one personal detail." : level === 3 ? "Sound calm, not rushed. Your listener trusts you more when your structure is clear even before your vocabulary becomes advanced." : "Authority comes from structure, pause control, and word choice. State the point first, then add nuance.";
const buildRevision = (level, lesson, globalOrderIndex) => ({ spaced_repetition_days: repetitionDays, review_targets: [globalOrderIndex > 1 ? `lesson-${globalOrderIndex - 1}` : null, globalOrderIndex > 3 ? `lesson-${globalOrderIndex - 3}` : null, `chapter-review-${lesson.chapter_id}`].filter(Boolean), retrieval_prompts: [`Retell the core idea of ${lesson.title.toLowerCase()} in two lines without looking.`, "Use three target words in one new sentence.", "Repeat the speaking task with one extra detail."], recommended_mode: level <= 2 ? "read-speak-write-review" : "recall-speak-compare-refine" });
const buildUnlockLogic = (level, chapterOrder, lessonOrder, previousLessonId) => ({ unlocked_by_default: level === 1 && chapterOrder === 1 && lessonOrder === 1, requires_completion_of: previousLessonId, chapter_unlock_rule: chapterOrder === 1 && lessonOrder === 1 ? "Start here. This lesson is unlocked by default." : lessonOrder === 1 ? `Complete all 10 lessons in chapter ${chapterOrder - 1} of level ${level}.` : `Complete lesson ${lessonOrder - 1} in this chapter.`, level_unlock_rule: level === 1 ? "Level 1 is available by default." : chapterOrder === 1 && lessonOrder === 1 ? `Complete every chapter in level ${level - 1}.` : `Remain active inside level ${level}.` });
function buildLesson(levelBlueprint, chapterBlueprint, lessonSeed, lessonOrder, globalOrderIndex, previousLessonId) { const patterns = buildPatternBank(lessonSeed[2], lessonSeed[1]); const lessonId = `L${levelBlueprint.level}-C${chapterBlueprint.chapter}-L${String(lessonOrder).padStart(2, "0")}`; const chapterId = `L${levelBlueprint.level}-C${chapterBlueprint.chapter}`; const vocabularyList = buildVocabulary(chapterBlueprint, lessonOrder - 1, patterns, levelBlueprint.level); const lesson = { level: levelBlueprint.level, level_title: levelBlueprint.title, cefr_band: levelBlueprint.cefrBand, chapter: chapterBlueprint.title, chapter_id: chapterId, lesson_id: lessonId, order_index: lessonOrder, global_order_index: globalOrderIndex, title: lessonSeed[0], learning_objective: `Use ${lessonSeed[2]} to ${lessonSeed[1]}.`, grammar_topic: { name: lessonSeed[2], ...buildGrammarNotes(lessonSeed[2]) }, vocabulary_list: vocabularyList, content: { explanation: buildExplanation(levelBlueprint.level, lessonSeed[2], lessonSeed[1]), sentence_patterns: patterns, real_life_conversation_examples: [] }, exercises: { speaking_practice_task: {}, writing_practice_task: {}, micro_drills: [] }, quiz: [], answers: [], common_mistakes: [], confidence_tip: "", revision: {}, unlock_logic: {}, chapter_summary: chapterBlueprint.summary }; lesson.content.real_life_conversation_examples = buildConversation(lesson, chapterBlueprint, patterns, levelBlueprint.level); lesson.exercises.speaking_practice_task = buildSpeakingTask(lesson, patterns, levelBlueprint.level); lesson.exercises.writing_practice_task = buildWritingTask(lesson, patterns); lesson.exercises.micro_drills = buildMicroDrills(patterns, vocabularyList); lesson.quiz = buildQuiz(lesson, patterns, vocabularyList); lesson.answers = lesson.quiz.map((item, index) => ({ question_number: index + 1, correct_answer: item.correct_answer })); lesson.common_mistakes = buildCommonMistakes(lessonSeed[2], lessonSeed[0]); lesson.confidence_tip = buildConfidenceTip(levelBlueprint.level, lesson); lesson.revision = buildRevision(levelBlueprint.level, lesson, globalOrderIndex); lesson.unlock_logic = buildUnlockLogic(levelBlueprint.level, chapterBlueprint.chapter, lessonOrder, previousLessonId); return lesson; }
function buildCourse() { const structure = { course_id: courseId, version, title: "Bolo English Complete Learning System", outcome: "A full journey from zero English to advanced, confident, professional English communication.", systems, levels: [] }; const lessons = []; let globalOrderIndex = 1; let previousLessonId = null; for (const levelBlueprint of blueprints) { const levelNode = { level: levelBlueprint.level, slug: levelBlueprint.slug, title: levelBlueprint.title, cefr_band: levelBlueprint.cefrBand, outcome: levelBlueprint.outcome, chapter_count: levelBlueprint.chapters.length, lesson_count: levelBlueprint.chapters.reduce((sum, chapter) => sum + chapter.lessons.length, 0), chapters: [] }; for (const chapterBlueprint of levelBlueprint.chapters) { const chapterNode = { chapter_id: `L${levelBlueprint.level}-C${chapterBlueprint.chapter}`, order_index: chapterBlueprint.chapter, title: chapterBlueprint.title, summary: chapterBlueprint.summary, lesson_titles: [] }; chapterBlueprint.lessons.forEach((lessonSeed, lessonIndex) => { chapterNode.lesson_titles.push({ lesson_id: `L${levelBlueprint.level}-C${chapterBlueprint.chapter}-L${String(lessonIndex + 1).padStart(2, "0")}`, order_index: lessonIndex + 1, title: lessonSeed[0] }); const lesson = buildLesson(levelBlueprint, chapterBlueprint, lessonSeed, lessonIndex + 1, globalOrderIndex, previousLessonId); lessons.push(lesson); previousLessonId = lesson.lesson_id; globalOrderIndex += 1; }); levelNode.chapters.push(chapterNode); } structure.levels.push(levelNode); } const completeCourse = { course_id: courseId, version, metadata: { total_levels: blueprints.length, total_chapters: blueprints.reduce((sum, level) => sum + level.chapters.length, 0), total_lessons: lessons.length, vocabulary_exposures: lessons.reduce((sum, lesson) => sum + lesson.vocabulary_list.length, 0), quiz_questions: lessons.reduce((sum, lesson) => sum + lesson.quiz.length, 0) }, systems, lessons }; return { structure, level1Lessons: lessons.filter((lesson) => lesson.level === 1), completeCourse, manifest: { course_id: courseId, version, phases: { phase_1: { file: "curriculum-phase-1-course-structure.json", description: "Full course structure with all levels, chapters, and lesson titles." }, phase_2: { file: "curriculum-phase-2-level-1-lessons.json", description: "Complete beginner level lessons with all required lesson fields." }, phase_3: { file: "curriculum-phase-3-complete-course.json", description: "Complete four-level curriculum with all 200 lessons." } }, api_endpoints: ["/api/curriculum/structure", "/api/curriculum/levels", "/api/curriculum/levels/:levelId", "/api/curriculum/lessons/:lessonId"] } }; }
const sqlEscape = (value) => String(value).replace(/'/g, "''");
function buildSqlSeed(structure, completeCourse) { const lines = ["BEGIN;", "DELETE FROM course_lessons;", "DELETE FROM course_chapters;", "DELETE FROM course_levels;"]; for (const level of structure.levels) { lines.push(`INSERT INTO course_levels (level_id, slug, title, cefr_band, outcome, chapter_count, lesson_count) VALUES (${level.level}, '${sqlEscape(level.slug)}', '${sqlEscape(level.title)}', '${sqlEscape(level.cefr_band)}', '${sqlEscape(level.outcome)}', ${level.chapter_count}, ${level.lesson_count});`); for (const chapter of level.chapters) { lines.push(`INSERT INTO course_chapters (chapter_id, level_id, order_index, title, summary) VALUES ('${sqlEscape(chapter.chapter_id)}', ${level.level}, ${chapter.order_index}, '${sqlEscape(chapter.title)}', '${sqlEscape(chapter.summary)}');`); } } for (const lesson of completeCourse.lessons) { lines.push(`INSERT INTO course_lessons (lesson_id, level_id, level_title, cefr_band, chapter_id, chapter_title, order_index, global_order_index, title, learning_objective, grammar_topic, content, vocabulary_list, exercises, quiz, answers, common_mistakes, confidence_tip, revision, unlock_logic) VALUES ('${sqlEscape(lesson.lesson_id)}', ${lesson.level}, '${sqlEscape(lesson.level_title)}', '${sqlEscape(lesson.cefr_band)}', '${sqlEscape(lesson.chapter_id)}', '${sqlEscape(lesson.chapter)}', ${lesson.order_index}, ${lesson.global_order_index}, '${sqlEscape(lesson.title)}', '${sqlEscape(lesson.learning_objective)}', '${sqlEscape(JSON.stringify(lesson.grammar_topic))}'::jsonb, '${sqlEscape(JSON.stringify(lesson.content))}'::jsonb, '${sqlEscape(JSON.stringify(lesson.vocabulary_list))}'::jsonb, '${sqlEscape(JSON.stringify(lesson.exercises))}'::jsonb, '${sqlEscape(JSON.stringify(lesson.quiz))}'::jsonb, '${sqlEscape(JSON.stringify(lesson.answers))}'::jsonb, '${sqlEscape(JSON.stringify(lesson.common_mistakes))}'::jsonb, '${sqlEscape(lesson.confidence_tip)}', '${sqlEscape(JSON.stringify(lesson.revision))}'::jsonb, '${sqlEscape(JSON.stringify(lesson.unlock_logic))}'::jsonb);`); } lines.push("COMMIT;"); return lines.join("\n"); }
const buildSchemaSql = () => `CREATE TABLE IF NOT EXISTS course_levels (\n  level_id INTEGER PRIMARY KEY,\n  slug TEXT NOT NULL UNIQUE,\n  title TEXT NOT NULL,\n  cefr_band TEXT NOT NULL,\n  outcome TEXT NOT NULL,\n  chapter_count INTEGER NOT NULL,\n  lesson_count INTEGER NOT NULL,\n  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()\n);\n\nCREATE TABLE IF NOT EXISTS course_chapters (\n  chapter_id TEXT PRIMARY KEY,\n  level_id INTEGER NOT NULL REFERENCES course_levels(level_id) ON DELETE CASCADE,\n  order_index INTEGER NOT NULL,\n  title TEXT NOT NULL,\n  summary TEXT NOT NULL,\n  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n  UNIQUE(level_id, order_index)\n);\n\nCREATE TABLE IF NOT EXISTS course_lessons (\n  lesson_id TEXT PRIMARY KEY,\n  level_id INTEGER NOT NULL REFERENCES course_levels(level_id) ON DELETE CASCADE,\n  level_title TEXT NOT NULL,\n  cefr_band TEXT NOT NULL,\n  chapter_id TEXT NOT NULL REFERENCES course_chapters(chapter_id) ON DELETE CASCADE,\n  chapter_title TEXT NOT NULL,\n  order_index INTEGER NOT NULL,\n  global_order_index INTEGER NOT NULL UNIQUE,\n  title TEXT NOT NULL,\n  learning_objective TEXT NOT NULL,\n  grammar_topic JSONB NOT NULL,\n  content JSONB NOT NULL,\n  vocabulary_list JSONB NOT NULL,\n  exercises JSONB NOT NULL,\n  quiz JSONB NOT NULL,\n  answers JSONB NOT NULL,\n  common_mistakes JSONB NOT NULL,\n  confidence_tip TEXT NOT NULL,\n  revision JSONB NOT NULL,\n  unlock_logic JSONB NOT NULL,\n  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),\n  UNIQUE(chapter_id, order_index)\n);\n\nCREATE INDEX IF NOT EXISTS idx_course_chapters_level_order ON course_chapters(level_id, order_index);\nCREATE INDEX IF NOT EXISTS idx_course_lessons_level_order ON course_lessons(level_id, global_order_index);\nCREATE INDEX IF NOT EXISTS idx_course_lessons_chapter_order ON course_lessons(chapter_id, order_index);\n`;
function buildTypeScriptModule(manifest, structure, level1Lessons, completeCourse) { return `export const curriculumManifest = ${JSON.stringify(manifest, null, 2)};\n\nexport const curriculumStructure = ${JSON.stringify(structure, null, 2)};\n\nexport const level1CurriculumLessons = ${JSON.stringify(level1Lessons, null, 2)};\n\nexport const completeCurriculumCourse = ${JSON.stringify(completeCourse, null, 2)};\n\nexport const curriculumLessonIndex = new Map(completeCurriculumCourse.lessons.map((lesson) => [lesson.lesson_id, lesson]));\n\nexport function getCurriculumLevel(levelId) { return curriculumStructure.levels.find((level) => level.level === levelId) ?? null; }\nexport function getCurriculumLesson(lessonId) { return curriculumLessonIndex.get(lessonId) ?? null; }\n`; }
const { manifest, structure, level1Lessons, completeCourse } = buildCourse(); mkdirSync(generatedDir, { recursive: true }); mkdirSync(dbGeneratedDir, { recursive: true }); writeFileSync(resolve(generatedDir, "curriculum-phase-1-course-structure.json"), JSON.stringify(structure, null, 2)); writeFileSync(resolve(generatedDir, "curriculum-phase-2-level-1-lessons.json"), JSON.stringify(level1Lessons, null, 2)); writeFileSync(resolve(generatedDir, "curriculum-phase-3-complete-course.json"), JSON.stringify(completeCourse, null, 2)); writeFileSync(resolve(generatedDir, "curriculum-manifest.json"), JSON.stringify(manifest, null, 2)); writeFileSync(resolve(generatedDir, "curriculum-data.ts"), buildTypeScriptModule(manifest, structure, level1Lessons, completeCourse)); writeFileSync(resolve(repoRoot, "db", "course-curriculum-schema.sql"), buildSchemaSql()); writeFileSync(resolve(dbGeneratedDir, "course-curriculum-seed.sql"), buildSqlSeed(structure, completeCourse));
